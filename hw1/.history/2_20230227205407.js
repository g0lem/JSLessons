// Write a program that asks the user for their name and greets them with their name.

let theirName = 'Geani';

let greeting = 'Hello, ' + theirName + '!';


if(theirName === 'Alice' || theirName === 'Bob') {
    console.log(greeting);
}
